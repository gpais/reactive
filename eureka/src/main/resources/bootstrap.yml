spring:
  application:
    name: eureka
  cloud:
    config:
      uri: http://config:8888

eureka:
  instance:
    prefer-ip-address: true
  client:
    registerWithEureka: false
    fetchRegistry: false
    server:
      waitTimeInMsWhenSyncEmpty: 0